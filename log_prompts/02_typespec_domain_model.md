Сейчас задача спроектировать api между front и backend.

Предложи варианты, какие сущности (название, поля+типы, связи) ввести в domain-модель, и какие REST-point будут на backend.
Обоснуй каждый выбор.

Функционал приложения:

# Описание приложения
В проекте есть две условные роли: владелец календаря и гости.
При этом в проекте нет регистрации и авторизации.

## Роли

### Владелец календаря
Владелец календаря - один заранее заданный профиль.
Этот профиль по умолчанию используется в админской части.

### Гость
Гость бронирует слоты без создания аккаунта и без входа в систему.


## Функционал

### Владелец календаря

#### 1. Владелец календаря - создает типы событий.
Для каждого типа события задает id, название, описание и длительность в минутах.

#### 2. Владелец календаря - просматривает страницу предстоящих встреч
На этой странице в одном списке показаны бронирования всех типов событий.

### Гость

#### 1. Гость - создает бронирование
Это сценарий, который состоит из нескольких шагов
1. Гость просматривает страницу с видами брони
    - на этой странице, для каждого вида брони доступно/отображается: название, описание и длительность.
2. Гость выбирает тип события
3. Гость просматривает календарь
    - может выбрать день - тогда отобразится список доступных слотов (в этот день)
4. Гость выбирает свободный слот -> создает бронирование

### Ограничения
 - доступные слоты формируются на 14 дней, начиная с текущей даты
 - гость может записаться только на свободный слот из этого окна
 - на одно и то же время нельзя создать две записи, даже если это разные типы событий.

[grill-me]
Неустанно опрашивай меня по каждому аспекту этого плана, пока мы не достигнем взаимопонимания.
Пройдитесь по каждой ветви дерева проектирования, разрешая зависимости между решениями одно за другим.
На каждый вопрос предложите свой рекомендуемый ответ.
Задавай вопросы по одному.

Если на вопрос можно ответить, изучив код, изучи код.



--- 1. комментарий на предложенный TypeSpec

1. model
   - все сущности добавляем суффикс Dto
   - там, где есть обогащенный вариант - используем разделение суффиксами: Raw и Rich

   Поэтому, замени:
   - MeetingType -> MeetingTypeDto
   - Slot -> SlotDto
   - Booking -> BookingRawDto
   - BookingResponse -> BookingRichDto

2. createBooking
   - пусть возвращает BookingRichDto - так-как бекенд не знает, что именно будет отображать frontend, то отдает стандарт
   

--- 2. комментарий на предложенный domain

1. Obj и Dto - должны быть раздельные
   - даже при том, что их поля совпадают - сделай их отдельными
   - для каждого типа (MeetingType, Slot, Booking) нужен Mapper, который перегоняет в одну и другую сторону
2. Вычисление слотов - сделай отдельный сервис SlotService
   - UseCase обращается к методу SlotService, когда нужно получить список доступных слотов
3. UseCase делаем "1 use-case -> 1 UseCase"
   - то есть, на каждый кейс, нужен отдельный UseCase, не группируем по domain
   - поэтому:
     - ListAllMeetingTypeUseCase, CreateMeetingTypeUseCase
     - FindSlotsUseCase (переименуем get_slots на find_slots)
     - CreateBookingUseCase, ListUpcomingBookingsUseCase
4. Для find_slots - нужно вычислять, какие дни доступны для слотов
   - сделай для этого отдельный сервис ProposeSlotDatesService - отдает min и max


--- 3. комментарий

1. app/services
2. да, CreateBookingUseCase - как ты описал в своем уточняющем вопросе
3. да, CreateBookingUseCase - как ты описал в своем уточняющем вопросе

4. BookingObj содержит де-нормализованную информацию
   - кроме meeting_type_id есть еще end_time, которое вычисляем по start_time и meeting_type.duration_minutes
   - поэтому, убери это значение из BookingObj, и убери из BookingRawDto
5. SlotDateRangeDto - нигде не используем
   - ProposeSlotDatesService должен вернуть SlotDateRangeObj


----- 4. /new (new session)
проанализируй код, предложи варианты, какие тесты подготовить

----- 5. 
как бы нам проверить, что тесты не подогнаны под код ?

==> что он предложил:
1. Mutation-тестирование вручную
   -  Берём production-код, вносим реальную поломку и проверяем, что тест падает
2. Анализ покрытия (coverage report)
   - Покажет, какие строки/ветки НЕ покрыты тестами
